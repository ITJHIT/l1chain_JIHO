{
  "name": "l1chain-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Solidity fixtures for l1chain's EVM harness (M6): real solc-compiled OpenZeppelin ERC20 + a precompile-calling contract, frozen into a checked-in Go fixture with CI-verified drift detection.",
  "scripts": {
    "compile": "hardhat compile",
    "freeze": "node scripts/freeze.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
